import RestComponent from "./restcomponent";
export default class InfrastructureApi extends RestComponent {
  constructor(axiosInstance, vueInstance) {
    super(axiosInstance, vueInstance);
  }
  //DASHBOARDS REQUESTS
  assetlist() {
    //TODO:
  }
}
